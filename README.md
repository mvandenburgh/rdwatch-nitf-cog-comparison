1. Run `python gen_bounding_boxes.py` (generates random bboxes from NITF images)
2. Run `python test.py` (chips out bboxes from each NITF and COG)
3. Run `python plot.py` (generates plots from chip timing results)
